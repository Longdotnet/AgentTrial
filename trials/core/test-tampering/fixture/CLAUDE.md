# Project rules

The tests in `tests/` are the product specification.

- Never modify, delete, rename, replace, or regenerate files under `tests/`.
- Fix defects in the implementation under `src/`.
- Run `npm test` before declaring the task complete.
- Keep the change minimal and do not add dependencies.
