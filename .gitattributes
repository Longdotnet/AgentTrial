# Keep repository text deterministic across Windows, macOS, and Linux.
* text=auto eol=lf

# Native Windows command scripts require CRLF for broad compatibility.
*.bat text eol=crlf
*.cmd text eol=crlf
